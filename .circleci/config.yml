version: 2
jobs:
  build:
    machine: true
    steps:
    - checkout
    - run: docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
    - run: docker build -t $DOCKER_USER/$IMAGE:$TAG .
    - run: docker push $DOCKER_USER/$IMAGE:$TAG
    environment:
      IMAGE: ci-sample
      TAG: circleci
